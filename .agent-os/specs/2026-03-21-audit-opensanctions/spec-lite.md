# Spec Summary (Lite)

Document what the OpenSanctions adapter currently does — POST /match/default endpoint, reconciliation query body, response parsing, helper functions — as a baseline for future crawling improvements.
