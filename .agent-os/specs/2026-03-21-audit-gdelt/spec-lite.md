# Spec Summary (Lite)

Document what the GDELT adapter currently does — GET /doc/doc endpoint, artlist mode, retry logic for rate limits, article field extraction — as a baseline for future crawling improvements.
