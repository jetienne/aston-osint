# Spec Summary (Lite)

After scan results return, the UI presents disambiguation questions (birth year, country, company) and match cards with accept/dismiss buttons. User answers trigger Claude re-evaluation of existing results without re-querying sources. Scans are stored temporarily (1 hour) to support iterative refinement.
